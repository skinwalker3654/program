#ifndef PROGRAM_H
#define PROGRAM_H

#define ARRAY_SIZE 10
extern int arr[ARRAY_SIZE];

/* Fnctions */
float find_avg(int arr[],int size);
int find_max(int arr[],int size);
int find_min(int arr[],int size);
void clear_buffer(void);

#endif
